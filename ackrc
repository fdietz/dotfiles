--no-css

# groups the results by file
--group

# only show one line of output context around the results
# --context=1

# follow symlinks
--follow

--color

--ignore-dir=gems
--ignore-dir=vendor
--ignore-dir=log
--ignore-dir=tmp
--ignore-dir=engines/perl_backend/test/stub_data
--ignore-dir=public/ass-xe


